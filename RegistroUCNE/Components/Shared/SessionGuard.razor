@inject NavigationManager Navigation
@inject SessionService session

@code {
    protected override void OnInitialized()
    {
        if (session.RegistradorId <= 0)
        {
            Navigation.NavigateTo("/", forceLoad: true);
        }
    }
}
